package com.ordevia.aidev.governance.domain;

public enum ToolCapability {
    READ,
    WRITE,
    EXECUTE,
    NETWORK,
    DATABASE_READ,
    DATABASE_WRITE,
    GIT_READ,
    GIT_WRITE,
    EXTERNAL_MUTATION,
    PRODUCTION
}
