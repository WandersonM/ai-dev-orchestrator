# Domain

A WorkItem represents a unit of software delivery entering the AI workflow. AgentExecution is an immutable audit trail of one agent attempt. Workflow transitions must be deterministic and validated by the orchestrator.
