# Conventions

- Java 25
- Spring Boot 4.1
- PostgreSQL
- Flyway for database migrations
- UUID for identifiers in MVP; UUIDv7 can replace generation in the next milestone
- REST APIs under `/api`
- Never use `double` for monetary values
- Prefer explicit domain transitions to status setters
